/* Construa um programa em que conte de 0 até 10 e
 * de 10 até zero, usando a estrutura de repetição "for"
 */

let input = require('readline-sync');

for(let contador = 0; contador <= 10; contador++){
    console.log(`Contador ${contador}`);
}