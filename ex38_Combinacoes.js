/* Elabore um programa em que mostre as combinações de três valores,
 * agrupado de dois em dois. Use a estrutura de repetição "for"
 */

for (let numeroA = 1; numeroA <= 3; numeroA++) {
    for (let numeroB = 1; numeroB <=3; numeroB++ ) {
        console.log(`${numeroA} - ${numeroB}`)
    }
}